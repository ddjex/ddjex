/**
 * ddjex Actions Compiler
 * (Handled in main compiler, this is for future expansion)
 */

export function compileActions(actions, compiler) {
  // Currently handled inline in Compiler.compileActions()
  return '';
}
